import styles from './Error.module.css';

export const Error = () => {
    return (
        <div className={styles.container}>
            <h2 className={styles.title}>
                Loading error
            </h2>
        </div>
    );
};